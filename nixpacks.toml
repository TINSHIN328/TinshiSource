[phases.setup]
nixPkgs = ["nodejs_22", "python312", "gcc"]

[phases.install]
cmds = ["npm ci --omit=dev", "python3 -m venv .control-venv", ".control-venv/bin/python -m pip install --disable-pip-version-check -q -r dc-bot/requirements.txt"]

[start]
cmd = "npm start"
